from nemere.inference.fieldTypes import ValueFieldTypeRecognizer

class BinaryProtocolsFieldTypeRecognizer(ValueFieldTypeRecognizer):
    """
    Class for future use in field type recognition.
    Provides the method #recognizedFields to find the most confident matches with the known field types to be recognized.
    """
    fieldtypeTemplates = []
